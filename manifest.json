{
  "id": "vggt_scene",
  "name": "VGGT Scene Reconstruction",
  "type": "model",
  "source": "https://github.com/facebookresearch/vggt",
  "description": "Feed-forward multi-view scene/room reconstruction (Meta VGGT-1B). Point it at a folder of overlapping photos of a room and it reconstructs the observed geometry — flat walls, real corners — then meshes it. Built for scenes, not single objects: do NOT remove backgrounds and provide several overlapping views. Outputs a colored mesh, and falls back to a point cloud if meshing is unavailable.",
  "version": "1.0.0",
  "author": "iammojogo",
  "vram_gb": 8,
  "tags": [
    "image-to-3d",
    "multi-view",
    "scene",
    "mesh"
  ],
  "nodes": [
    {
      "id": "generate",
      "name": "Reconstruct Scene",
      "inputs": [
        "image"
      ],
      "hf_repo": "facebook/VGGT-1B",
      "download_check": "model.pt",
      "hf_skip_prefixes": [],
      "params_schema": [
        {
          "id": "images_dir",
          "label": "Images Folder",
          "type": "string",
          "default": "",
          "tooltip": "Folder containing the overlapping photos of the room/scene. Every image in this folder is used. If left empty, only the single image wired into the node is used (poor for rooms)."
        },
        {
          "id": "max_views",
          "label": "Max Views",
          "type": "select",
          "default": 24,
          "options": [
            { "value": 8,  "label": "8" },
            { "value": 16, "label": "16" },
            { "value": 24, "label": "24 (8 GB safe)" },
            { "value": 32, "label": "32" },
            { "value": 48, "label": "48 (needs more VRAM)" }
          ],
          "tooltip": "Caps how many photos are fed to the model. VGGT memory grows with view count; lower this if you hit out-of-memory."
        },
        {
          "id": "conf_percentile",
          "label": "Confidence Filter",
          "type": "select",
          "default": "50.0",
          "options": [
            { "value": "0.0",  "label": "Off (keep all)" },
            { "value": "30.0", "label": "Light (30%)" },
            { "value": "50.0", "label": "Default (50%)" },
            { "value": "70.0", "label": "Strict (70%)" },
            { "value": "85.0", "label": "Very strict (85%)" }
          ],
          "tooltip": "Drops the lowest-confidence depth points before meshing. Higher removes more noise and floaters but thins the cloud."
        },
        {
          "id": "output_mode",
          "label": "Output",
          "type": "select",
          "default": "mesh",
          "options": [
            { "value": "mesh",       "label": "Mesh (Poisson)" },
            { "value": "pointcloud", "label": "Point cloud only" }
          ],
          "tooltip": "Mesh runs Poisson reconstruction over the points. Point cloud exports the raw colored points (most faithful to exactly what was observed)."
        },
        {
          "id": "voxel_size",
          "label": "Downsample",
          "type": "select",
          "default": "0.01",
          "options": [
            { "value": "0",     "label": "Off" },
            { "value": "0.005", "label": "Fine (0.005)" },
            { "value": "0.01",  "label": "Default (0.01)" },
            { "value": "0.02",  "label": "Coarse (0.02)" }
          ],
          "tooltip": "Voxel size for thinning the cloud before meshing. Larger = fewer points, faster, smoother."
        },
        {
          "id": "poisson_depth",
          "label": "Mesh Detail",
          "type": "select",
          "default": 9,
          "options": [
            { "value": 8,  "label": "Low (8)" },
            { "value": 9,  "label": "Default (9)" },
            { "value": 10, "label": "High (10)" },
            { "value": 11, "label": "Higher (11)" }
          ],
          "tooltip": "Poisson octree depth. Higher = more detail but more memory and more spurious surface."
        },
        {
          "id": "density_quantile",
          "label": "Trim Low Density",
          "type": "select",
          "default": "0.02",
          "options": [
            { "value": "0.0",  "label": "Off" },
            { "value": "0.02", "label": "Default (2%)" },
            { "value": "0.05", "label": "More (5%)" },
            { "value": "0.1",  "label": "Aggressive (10%)" }
          ],
          "tooltip": "Removes the lowest-density Poisson vertices, trimming the inflated 'bubble' Poisson adds around sparse areas."
        },
        {
          "id": "clean_floaters",
          "label": "Keep Largest Piece",
          "type": "select",
          "default": "true",
          "options": [
            { "value": "true",  "label": "Yes" },
            { "value": "false", "label": "No" }
          ],
          "tooltip": "Discards small disconnected mesh fragments, keeping only the largest connected surface."
        },
        {
          "id": "postprocess",
          "label": "Planar Cleanup",
          "type": "select",
          "default": "none",
          "options": [
            { "value": "none",          "label": "Off (raw mesh / points)" },
            { "value": "ransac_planes", "label": "RANSAC planes (flatten surfaces)" },
            { "value": "manhattan",     "label": "Manhattan snap (square 90° corners)" }
          ],
          "tooltip": "Fits dominant flat planes (walls/floor/ceiling) to the point cloud. 'RANSAC planes' rebuilds each surface as a genuinely flat quad. 'Manhattan snap' also rotates them onto orthogonal axes so corners meet at true 90 degrees. Both replace the Poisson mesh; if no planes are found it exports the point cloud."
        },
        {
          "id": "plane_tolerance",
          "label": "Plane Tolerance",
          "type": "select",
          "default": "0.012",
          "options": [
            { "value": "0.006", "label": "Tight" },
            { "value": "0.012", "label": "Default" },
            { "value": "0.025", "label": "Loose (noisy data)" }
          ],
          "tooltip": "RANSAC inlier distance as a fraction of the scene size. Larger merges more points into each plane (good for noisy clouds); smaller keeps planes thin but may split a single wall in two."
        },
        {
          "id": "max_planes",
          "label": "Max Planes",
          "type": "select",
          "default": 10,
          "options": [
            { "value": 6,  "label": "6 (bare room)" },
            { "value": 10, "label": "10 (default)" },
            { "value": 15, "label": "15" },
            { "value": 20, "label": "20 (cluttered)" }
          ],
          "tooltip": "How many flat surfaces to extract, largest first. A bare room is ~6 (4 walls + floor + ceiling); raise it if furniture or shelves should also become planes."
        }
      ],
      "output": "mesh",
      "input": "image"
    }
  ],
  "generator_class": "VGGTSceneGenerator"
}
